import React, { useMemo, useState } from "react";
import { HiArrowUpRight } from "react-icons/hi2";
import { Link, useNavigate, useParams } from "react-router-dom";
import Card from "./component/ui/Card";
import { useCart } from "./context/CartContext";
import { formatPrice, getProductById, products } from "./data/products";

export default function ProductDetail() {
  const { id } = useParams();
  const navigate = useNavigate();
  const { addToCart } = useCart();
  const [selectedSize, setSelectedSize] = useState("");
  const [activeTab, setActiveTab] = useState("Details");
  const product = useMemo(() => getProductById(id), [id]);
  const related = useMemo(() => products.filter((item) => item.id !== id).slice(0, 3), [id]);
  const details = { Details: "A relaxed everyday essential made for repeated wear. Thoughtful construction, comfortable finish and a silhouette that works with everything.", "Size & fit": "Designed for a relaxed fit. Choose your regular size; size up for a more oversized look.", "Delivery & returns": "Complimentary shipping on orders over $200. Returns are accepted within 30 days of delivery." };

  if (!product) return <main className="site-container py-32"><h1 className="display text-4xl">Product not found</h1><Link to="/shop" className="editorial-link mt-6">Back to shop</Link></main>;

  return <main className="site-container py-7 md:py-10"><nav className="mb-7 text-sm text-muted"><Link to="/">Home</Link> <span className="px-2">/</span><Link to="/shop">Shop</Link> <span className="px-2">/</span><span className="text-black">{product.name}</span></nav><div className="grid gap-8 lg:grid-cols-[minmax(0,1.45fr)_minmax(330px,.7fr)] lg:gap-16"><div className="grid gap-4 sm:grid-cols-2"><div className="aspect-[4/5] overflow-hidden rounded-[14px] bg-neutral"><img src={product.images?.[0] || product.image} alt={product.name} className="h-full w-full object-cover" /></div><div className="aspect-[4/5] overflow-hidden rounded-[14px] bg-neutral"><img src={product.images?.[1] || product.hoverImage || product.image} alt={`${product.name} alternate`} className="h-full w-full object-cover" /></div></div><aside className="lg:pt-3"><p className="eyebrow">{product.category}</p><h1 className="display mt-3 text-4xl">{product.name}</h1><p className="mt-4 text-lg">{formatPrice(product.price)}</p><div className="my-8 border-y border-black/10 py-6"><div className="mb-4 flex justify-between text-sm"><span>Size</span><button className="underline">Size guide</button></div><div className="grid grid-cols-4 gap-2">{["S", "M", "L", "XL"].map((size) => <button key={size} onClick={() => setSelectedSize(size)} className={`border py-3 text-sm transition ${selectedSize === size ? "border-black bg-black text-white" : "border-black/15 hover:border-black"}`}>{size}</button>)}</div></div><button onClick={() => selectedSize && addToCart(product, 1)} className={`w-full rounded-full py-4 text-sm font-medium transition ${selectedSize ? "bg-[#e85d04] text-white hover:bg-[#c94d00]" : "cursor-not-allowed bg-neutral text-muted"}`}>{selectedSize ? "Add to bag" : "Select a size"}</button><p className="mt-4 text-center text-xs text-muted">Free shipping on orders over $200</p><div className="mt-9 border-t border-black/10">{Object.entries(details).map(([tab, copy]) => <div key={tab} className="border-b border-black/10"><button onClick={() => setActiveTab(activeTab === tab ? "" : tab)} className="flex w-full items-center justify-between py-5 text-left text-sm"><span>{tab}</span><span className="text-xl font-light">{activeTab === tab ? "−" : "+"}</span></button>{activeTab === tab && <p className="max-w-md pb-5 text-sm leading-6 text-muted">{copy}</p>}</div>)}</div></aside></div><section className="py-24 md:py-32"><div className="mb-9 flex items-end justify-between"><div><p className="eyebrow">Keep exploring</p><h2 className="display mt-3 text-4xl">You may also like</h2></div><Link to="/shop" className="editorial-link">View all <HiArrowUpRight aria-hidden="true" /></Link></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{related.map((item) => <div className="cursor-pointer" key={item.id} onClick={() => navigate(`/shop/${item.id}`)}><Card img={item.image} hoverImg={item.hoverImage} title={item.name} price={formatPrice(item.price)} onQuickAdd={() => addToCart(item, 1)} /></div>)}</div></section></main>;
}
